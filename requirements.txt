# Core dependencies for MeshAI MCP Server
httpx>=0.25.0
structlog>=23.2.0
pydantic>=2.5.0

# HTTP Server support
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Optional: Official MCP package (when available)
# mcp>=0.1.0

# For WebSocket support and async operations
websockets>=11.0.0
asyncio-mqtt>=0.13.0

# For retry logic and robust HTTP operations
tenacity>=8.2.0

# For environment variable management
python-dotenv>=1.0.0

# For CLI interface (if running standalone)
click>=8.0.0
rich>=13.0.0

# Authentication client dependencies (safe - no sensitive code)
cachetools>=5.3.0
python-multipart>=0.0.6

# Gateway client dependencies
aiohttp>=3.9.0

# Configuration management
PyYAML>=6.0

# Database support
asyncpg>=0.29.0
sqlalchemy>=2.0.0